<h2>ジャンル編集</h2>

<%= form_with model: @genre, url: admin_genre_path(@genre), submit: '更新' do |f| %>
  <%= f.label :name, "ジャンル名" %>
  <%= f.text_field :name %>
	<%= f.submit '更新', class: "btn btn-primary ml2" %>
<% end %>